const readlineSync = require("readline-sync");

let arrfirst = ["tableau", "craie", "effaceur"];

for (let elem of arrfirst) {
  	console.log("Voici la copie d'un mot du premier tableau : " + elem);
}